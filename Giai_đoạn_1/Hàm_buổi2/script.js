// Cách khai báo hàm
/*
let getTotal = (a, b) => {
  // arrow function
  let total = a + b;
  return total;
};

let c = getTotal(4, 56);
document.write(c);

let getTotal1 = (a, b) => a + b;
let c1 = getTotal1(4, 6);
document.write(c1);

let arr = [1, 4, 5, 6, 8, 9];
let newArr = arr.filter(function loc(item) {
  if (item % 2 == 0) {
    return true;
  } else {
    return false;
  }
});
console.log(newArr);

let newArr1 = arr.filter((item) => item % 2 == 0);
console.log(newArr1);

let showName = (name) => document.write("name");
*/
